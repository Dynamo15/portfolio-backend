# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=portfolio_db
DB_USER=postgres
DB_PASSWORD=change_me

# Optional: overrides the individual DB_* values, useful for managed PostgreSQL.
# DATABASE_URL=postgresql+psycopg://user:password@host:5432/database

# Application
APP_NAME=Portfolio API
APP_VERSION=1.0.0
DEBUG=True
CORS_ORIGINS=http://localhost:5173
